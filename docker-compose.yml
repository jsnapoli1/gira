version: '3.8'

services:
  zira:
    build:
      context: .
      args:
        GIT_SHA: local
    ports:
      - "9002:9002"
    volumes:
      - zira-data:/app/data
    environment:
      - PORT=9002
      - DB_PATH=/app/data/zira.db
    restart: unless-stopped

volumes:
  zira-data:
    driver: local
